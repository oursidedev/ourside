"use client";import { useState } from "react";import { Bell,LoaderCircle } from "lucide-react";import { useToast } from "@/components/ui/ToastProvider";
export function TestNotificationButton(){const[busy,setBusy]=useState(false);const{toast}=useToast();async function send(){setBusy(true);const response=await fetch("/api/admin/notifications/test",{method:"POST"});const data=await response.json();toast(response.ok?"Test notification sent to your account.":data.error,response.ok?"success":"error");setBusy(false)}return <button disabled={busy} onClick={send} className="focus-ring inline-flex min-h-11 items-center gap-2 rounded-full bg-wine px-4 text-sm font-bold text-white">{busy?<LoaderCircle className="h-4 w-4 animate-spin"/>:<Bell className="h-4 w-4"/>}Send test notification</button>}
