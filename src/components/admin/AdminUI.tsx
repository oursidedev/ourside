import { ArrowUpRight } from "lucide-react";import Link from "next/link";
export { RefreshButton } from "./AdminRefreshButton";
export function AdminHeader({eyebrow="Ourside operations",title,description,action}:{eyebrow?:string;title:string;description:string;action?:React.ReactNode}){return <header className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-wine">{eyebrow}</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">{title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-ink/50">{description}</p></div>{action}</header>}
export function MetricCard({label,value,hint,href}:{label:string;value:React.ReactNode;hint?:string;href?:string}){const body=<div className="rounded-2xl border bg-paper p-5 shadow-card"><p className="text-xs font-bold uppercase tracking-wider text-ink/40">{label}</p><p className="mt-3 font-sans text-3xl font-extrabold tracking-tight">{value??"—"}</p>{hint&&<p className="mt-2 text-xs text-ink/45">{hint}</p>}</div>;return href?<Link href={href} className="group relative block">{body}<ArrowUpRight className="absolute right-4 top-4 h-4 w-4 text-wine opacity-0 transition group-hover:opacity-100"/></Link>:body}
export function DataTable({rows}:{rows:Record<string,unknown>[]}){if(!rows.length)return <div className="mt-8 rounded-2xl border border-dashed bg-paper p-10 text-center"><p className="font-serif text-2xl">Nothing to review yet.</p><p className="mt-2 text-sm text-ink/45">Operational records will appear here as Ourside is used.</p></div>;const columns=Object.keys(rows[0]).filter(key=>!['metadata'].includes(key)).slice(0,9);return <div className="mt-8 overflow-hidden rounded-2xl border bg-paper shadow-card"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-cream text-[11px] uppercase tracking-wider text-ink/45"><tr>{columns.map(column=><th key={column} className="whitespace-nowrap px-4 py-3">{column.replaceAll('_',' ')}</th>)}</tr></thead><tbody className="divide-y">{rows.map((row,index)=><tr key={String(row.id||index)} className="hover:bg-wine/[.025]">{columns.map(column=><td key={column} className="max-w-xs truncate whitespace-nowrap px-4 py-4 text-ink/65">{formatCell(row[column])}</td>)}</tr>)}</tbody></table></div></div>}
function formatCell(value:unknown){if(value==null)return"—";if(typeof value==="boolean")return value?"Yes":"No";if(typeof value==="object")return JSON.stringify(value);const text=String(value);return /^\d{4}-\d{2}-\d{2}T/.test(text)?new Date(text).toLocaleString():text}
