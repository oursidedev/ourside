"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { LoaderCircle } from "lucide-react";import { useToast } from "@/components/ui/ToastProvider";
export function ErrorTable({rows}:{rows:Record<string,unknown>[]}){const [busy,setBusy]=useState<string|null>(null);const {toast}=useToast();const router=useRouter();async function update(id:string,status:string){setBusy(id);const response=await fetch(`/api/admin/errors/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status})});toast(response.ok?"Error status updated.":"Status update failed.",response.ok?"success":"error");setBusy(null);router.refresh()}if(!rows.length)return <div className="mt-8 rounded-2xl border border-dashed bg-paper p-10 text-center">No captured errors.</div>;return <div className="mt-8 space-y-3">{rows.map(row=><article key={String(row.id)} className="rounded-2xl border bg-paper p-5 shadow-card"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-wider text-red-700">{String(row.level)} · {String(row.status)}</p><h2 className="mt-2 truncate font-bold">{String(row.message)}</h2><p className="mt-2 text-xs text-ink/45">{String(row.path||row.source||'Unknown source')} · {String(row.occurrence_count||1)} occurrence(s)</p></div><div className="flex flex-wrap gap-2">{busy===row.id?<LoaderCircle className="h-5 w-5 animate-spin"/>:['investigating','resolved','ignored','open'].map(status=><button key={status} onClick={()=>update(String(row.id),status)} className="min-h-9 rounded-full border px-3 text-xs font-bold hover:bg-wine/5">{status}</button>)}</div></div></article>)}</div>}
