/** Cross-browser clipboard adapter with a DOM fallback for older mobile browsers. */
export async function copyToClipboard(text:string):Promise<{ok:true}|{ok:false;error:string}>{if(!text)return{ok:false,error:"Nothing to copy."};try{if(typeof navigator!=="undefined"&&navigator.clipboard&&window.isSecureContext){await navigator.clipboard.writeText(text);return{ok:true};}if(typeof document==="undefined")return{ok:false,error:"Clipboard is unavailable."};const area=document.createElement("textarea");area.value=text;area.readOnly=true;area.style.position="fixed";area.style.opacity="0";document.body.appendChild(area);area.select();area.setSelectionRange(0,text.length);const ok=document.execCommand("copy");area.remove();return ok?{ok:true}:{ok:false,error:"Copy was blocked by this browser."};}catch(error){return{ok:false,error:error instanceof Error?error.message:"Copy failed."};}}
