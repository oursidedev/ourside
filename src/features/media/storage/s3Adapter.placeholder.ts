/** Part 3+ placeholder: implement with server-issued presigned URLs; never ship AWS credentials in web/native bundles. */
import type { StorageAdapter } from "../storageAdapter";
export type S3StorageAdapter = StorageAdapter;
