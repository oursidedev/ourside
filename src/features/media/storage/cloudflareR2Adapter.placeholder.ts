/** Part 3+ placeholder: implement with server-issued presigned URLs; never expose R2 credentials to clients. */
import type { StorageAdapter } from "../storageAdapter";
export type CloudflareR2StorageAdapter = StorageAdapter;
