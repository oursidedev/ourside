/** TODO: Validate Apple IAP transactions server-side when iOS launches. Store transaction IDs, never trust device-only state. */
import type { BillingProviderAdapter } from "../billing.provider"; export type AppleBillingProvider = BillingProviderAdapter;
