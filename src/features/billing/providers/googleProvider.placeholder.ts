/** TODO: Validate Google Play purchase tokens server-side when Android launches and map them to shared subscriptions. */
import type { BillingProviderAdapter } from "../billing.provider"; export type GoogleBillingProvider = BillingProviderAdapter;
