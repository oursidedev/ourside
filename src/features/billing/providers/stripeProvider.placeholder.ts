/** TODO: Add Stripe Checkout/Portal only if available; raw card data must never pass through Ourside servers. */
import type { BillingProviderAdapter } from "../billing.provider"; export type StripeBillingProvider = BillingProviderAdapter;
