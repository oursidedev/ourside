/** TODO: Add Paddle server checkout and verified webhooks. Load Paddle.js only on checkout, never globally. */
import type { BillingProviderAdapter } from "../billing.provider"; export type PaddleBillingProvider = BillingProviderAdapter;
