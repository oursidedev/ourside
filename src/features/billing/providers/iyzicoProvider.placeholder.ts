/** TODO: Add iyzico checkout/webhook verification for Turkish web billing. Keep card fields on iyzico-hosted surfaces. */
import type { BillingProviderAdapter } from "../billing.provider"; export type IyzicoBillingProvider = BillingProviderAdapter;
