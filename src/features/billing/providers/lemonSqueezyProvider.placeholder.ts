/** TODO: Add Lemon Squeezy checkout and signature-verified webhook mapping behind BillingProviderAdapter. */
import type { BillingProviderAdapter } from "../billing.provider"; export type LemonSqueezyBillingProvider = BillingProviderAdapter;
