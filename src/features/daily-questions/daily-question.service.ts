import type { SubmitDailyAnswerInput } from "./daily-question.schema"; import type { DailyAnswer, DailyAnswerReveal, DailyQuestion } from "./daily-question.types";
export interface DailyQuestionRepository { current(locale: string): Promise<DailyQuestion | null>; answer(userId: string, input: SubmitDailyAnswerInput): Promise<DailyAnswer>; reveal(coupleId: string, questionId: string, answerDate: string): Promise<DailyAnswerReveal>; }
export class DailyQuestionService { constructor(private readonly repository: DailyQuestionRepository) {} current(locale: string) { return this.repository.current(locale); } answer(userId: string, input: SubmitDailyAnswerInput) { return this.repository.answer(userId, input); } reveal(coupleId: string, questionId: string, date: string) { return this.repository.reveal(coupleId, questionId, date); } }
