import { AdminHeader,DataTable } from "@/components/admin/AdminUI";import { getAdminIdentity,getAdminUsers } from "@/features/admin/admin.server";
export default async function AdminSettings(){const [identity,admins]=await Promise.all([getAdminIdentity(),getAdminUsers()]);return <><AdminHeader title="Admin settings" description={`Signed in as ${identity?.email}. Only owners should change internal access roles.`}/><section className="mt-8 rounded-2xl border bg-paper p-5 shadow-card"><p className="text-xs font-bold uppercase tracking-wider text-wine">Provider modes</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><Status label="Billing" value={process.env.BILLING_PROVIDER||'mock'}/><Status label="Email" value={process.env.EMAIL_PROVIDER||'mock'}/><Status label="Analytics" value={process.env.NEXT_PUBLIC_ANALYTICS_ENABLED==='false'?'disabled':'enabled'}/></div></section><DataTable rows={admins as unknown as Record<string,unknown>[]}/></>}
function Status({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-cream p-4"><p className="text-xs text-ink/40">{label}</p><p className="mt-1 font-bold">{value}</p></div>}
