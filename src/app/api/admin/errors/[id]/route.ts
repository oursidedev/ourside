import { NextRequest,NextResponse } from "next/server";import { z } from "zod";import { requireAdmin,writeAudit } from "@/features/admin/admin.server";import { createServerSupabaseClient } from "@/lib/supabase/server";
const schema=z.object({status:z.enum(["open","investigating","resolved","ignored"])});export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;const {status}=schema.parse(await request.json());const db=await createServerSupabaseClient();const {error}=await db!.from("app_errors").update({status}).eq("id",id);if(error)throw error;await writeAudit(`mark_error_${status}`,"app_error",id);return NextResponse.json({ok:true});}catch(cause){return NextResponse.json({error:cause instanceof Error?cause.message:"Update failed"},{status:400});}}
