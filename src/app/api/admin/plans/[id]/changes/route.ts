import { NextResponse } from "next/server";import { requireAdmin } from "@/features/admin/admin.server";import { createServerSupabaseClient } from "@/lib/supabase/server";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;const db=await createServerSupabaseClient();const {data,error}=await db!.from("subscription_plan_changes").select("id,admin_email,change_type,reason,created_at").eq("plan_id",id).order("created_at",{ascending:false}).limit(30);if(error)throw error;return NextResponse.json({changes:data||[]});}catch{return NextResponse.json({error:"Not found"},{status:404});}}
